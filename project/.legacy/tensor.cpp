// #include "tensor.h"

// template <typename T>
// Tensor<T>::Tensor(size_t ndim, size_t* shape, T* data)
//     : ndim(ndim), shape(shape), data(data) {}

// template <typename T>
// Tensor<T>::~Tensor() {
//     delete[] shape;
//     // delete[] data; Don't delete data as it belongs to Python
// }
